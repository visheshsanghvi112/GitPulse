version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.example
    volumes:
      - ./:/app
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

